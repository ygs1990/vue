/**
 * Created by zjw on 2017/6/5.
 */
class getSid {
  constructor() {
    this.sid = this.getTime();
  }
  getTime() {
    let timestamp = parseInt(new Date().getTime());
    let random = Math.floor(Math.random());
    return timestamp + random
  }
}

//导出一个对象
export default new getSid();
